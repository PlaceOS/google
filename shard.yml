name: google
version: 3.0.0

dependencies:
  connect-proxy:
    github: spider-gazelle/connect-proxy
    version: ~> 1.0
  jwt:
    github: crystal-community/jwt
    version: ~> 1.4

development_dependencies:
  webmock:
    github: manastech/webmock.cr
    version: 0.13.0 # remove once 0.36.0 released

  # Include this in your projects shard file if you want to send emails via gmail API
  email:
    github: arcage/crystal-email
